<div class="bg-indigo-600 text-white shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Left: Brand -->
      <div class="flex-shrink-0">
        <%= link_to "📚 BookHaven", root_path, class: "text-2xl font-bold tracking-tight hover:text-indigo-200 transition" %>
      </div>

      <!-- Right: Navigation -->
      <% if user_signed_in? %>
      <div class="flex items-center space-x-4">
        <%= link_to "View Libraries", libraries_path, class: "text-white hover:text-indigo-200 font-medium transition" %>

        <%= button_to "Logout", destroy_user_session_path,
              method: :delete,
              class: "bg-white text-indigo-700 font-semibold px-3 py-1.5 rounded-md hover:bg-indigo-100 transition" %>
      </div>
      <% else %>
      <div class="flex items-center space-x-4">
        <%= link_to "Login", new_user_session_path, class: "text-white hover:text-indigo-200 font-medium transition" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "bg-white text-indigo-700 font-semibold px-3 py-1.5 rounded-md hover:bg-indigo-100 transition" %>
      </div>
      <% end %>
    </div>
  </div>
</div>
